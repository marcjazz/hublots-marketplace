steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'ghcr.io/${_GITHUB_OWNER}/${_GITHUB_REPO_NAME}/vendor-panel:${_TAG}', '.']
    dir: 'vendor-panel'
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'ghcr.io/${_GITHUB_OWNER}/${_GITHUB_REPO_NAME}/vendor-panel:${_TAG}']
    dir: 'vendor-panel'
images:
  - 'ghcr.io/${_GITHUB_OWNER}/${_GITHUB_REPO_NAME}/vendor-panel:${_TAG}'
